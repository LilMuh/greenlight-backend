# Gradle daemon 用 UTF-8，否则它会用 Windows 本地编码（GBK）去解码 bootRun 子进程的输出，中文日志变乱码。
# file.encoding 不够：daemon 读子进程输出流用的是 sun.jnu.encoding / native.encoding，
# 这两个跟随系统 ANSI 代码页，得单独指定。
org.gradle.jvmargs=-Dfile.encoding=UTF-8 -Dsun.jnu.encoding=UTF-8 -Dnative.encoding=UTF-8 -Xmx2g
